# header.scss
{
    justify-content: unset
    .header.shrink: 
    > * ~ *:
}

# header.jsx
{
    useEffect:
    useRef:
    useLocation:
    link:
    findIndex:
    document.body.scrollTop:
    document.documentElement.scrollTop:
    headerRef.current:
}